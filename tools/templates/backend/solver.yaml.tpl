# {{PROJECT_NAME}} solver configuration
# Consumed by the isolated Solver package (OR-Tools CP-SAT).

solver:
  engine: cp-sat
  max_time_in_seconds: 30
  num_search_workers: 8
  log_search_progress: false
